{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://braingraph.local/schemas/workspace.schema.json",
  "title": "Braingraph workspace",
  "type": "object",
  "required": [
    "$schema",
    "schemaVersion",
    "templateVersion",
    "workspace",
    "knowledge",
    "externalSystems",
    "repositories"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "templateVersion": { "type": "integer", "minimum": 1, "maximum": 2 },
    "workspace": {
      "type": "object",
      "required": [
        "name",
        "slug",
        "description",
        "scope",
        "sensitivity",
        "profiles"
      ],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "slug": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
        "description": {
          "type": "string",
          "minLength": 1,
          "pattern": "^.*\\S.*$"
        },
        "scope": {
          "enum": [
            "project",
            "organization",
            "professional-domain",
            "personal-domain",
            "mixed"
          ]
        },
        "sensitivity": {
          "enum": ["public", "private", "confidential", "regulated"]
        },
        "profiles": {
          "type": "array",
          "items": { "enum": ["knowledge", "software"] },
          "contains": { "const": "knowledge" },
          "uniqueItems": true
        }
      }
    },
    "knowledge": {
      "type": "object",
      "required": ["directory", "maintenance", "obsidian", "qmd"],
      "additionalProperties": false,
      "properties": {
        "directory": { "$ref": "#/$defs/knowledgeDirectory" },
        "maintenance": {
          "type": "object",
          "required": ["mode"],
          "additionalProperties": false,
          "properties": {
            "mode": { "enum": ["proposal-first", "delegated"] },
            "delegatedScope": { "type": "string", "minLength": 1 }
          },
          "allOf": [
            {
              "if": {
                "type": "object",
                "properties": { "mode": { "const": "delegated" } },
                "required": ["mode"]
              },
              "then": {
                "properties": { "delegatedScope": {} },
                "required": ["delegatedScope"]
              },
              "else": { "not": { "required": ["delegatedScope"] } }
            }
          ]
        },
        "obsidian": {
          "type": "object",
          "required": ["enabled", "vaultName"],
          "additionalProperties": false,
          "properties": {
            "enabled": { "const": true },
            "vaultName": { "type": "string", "minLength": 1 }
          }
        },
        "qmd": {
          "type": "object",
          "required": ["enabled", "collection", "include"],
          "additionalProperties": false,
          "properties": {
            "enabled": { "const": true },
            "collection": { "type": "string", "minLength": 1 },
            "include": {
              "type": "array",
              "items": { "type": "string", "minLength": 1 },
              "minItems": 1,
              "uniqueItems": true
            }
          }
        }
      }
    },
    "externalSystems": {
      "type": "array",
      "$comment": "External-system id uniqueness is enforced by the runtime validator because JSON Schema cannot express uniqueness by one object property.",
      "items": { "$ref": "#/$defs/externalSystem" }
    },
    "repositories": {
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      },
      "additionalProperties": { "$ref": "#/$defs/repository" }
    }
  },
  "$defs": {
    "systemIdentity": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "properties": {
        "account": { "$ref": "#/$defs/identityString" },
        "tenant": { "$ref": "#/$defs/identityString" },
        "principal": { "$ref": "#/$defs/identityString" }
      }
    },
    "identityString": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?![\\s])(?!.*[\\s]$)[^\\u0000-\\u001f\\u007f]+$"
    },
    "externalSystem": {
      "type": "object",
      "required": [
        "id",
        "name",
        "status",
        "roles",
        "owns",
        "identifiers",
        "access",
        "freshness",
        "capture",
        "sensitivity",
        "fallback"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
        "name": { "type": "string", "minLength": 1 },
        "status": { "enum": ["active", "planned", "inactive"] },
        "url": { "type": "string", "minLength": 1 },
        "roles": {
          "type": "array",
          "items": {
            "enum": [
              "source",
              "intake",
              "execution",
              "communication",
              "reference",
              "archive"
            ]
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "owns": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1,
          "uniqueItems": true
        },
        "identifiers": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1,
          "uniqueItems": true
        },
        "identity": { "$ref": "#/$defs/systemIdentity" },
        "access": {
          "type": "object",
          "required": ["read", "write"],
          "additionalProperties": false,
          "properties": {
            "read": { "enum": ["none", "manual", "connector"] },
            "write": { "enum": ["prohibited", "human-approval", "delegated"] }
          }
        },
        "writeScope": { "type": "string", "minLength": 1 },
        "freshness": {
          "enum": [
            "verify-live",
            "revision-tracked",
            "snapshot",
            "not-applicable"
          ]
        },
        "capture": {
          "enum": ["link", "summarize", "synchronize", "copy", "exclude"]
        },
        "sensitivity": {
          "enum": ["public", "private", "confidential", "regulated"]
        },
        "fallback": { "type": "string", "minLength": 1 },
        "notes": { "type": "string" }
      },
      "allOf": [
        {
          "if": {
            "type": "object",
            "properties": {
              "access": {
                "type": "object",
                "properties": { "write": { "const": "delegated" } },
                "required": ["write"]
              }
            }
          },
          "then": {
            "properties": { "writeScope": {} },
            "required": ["writeScope"]
          }
        },
        {
          "if": {
            "type": "object",
            "properties": {
              "access": {
                "type": "object",
                "properties": { "write": { "const": "delegated" } },
                "required": ["write"]
              }
            }
          },
          "else": { "not": { "required": ["writeScope"] } }
        }
      ]
    },
    "repository": {
      "type": "object",
      "required": [
        "mode",
        "url",
        "path",
        "integrationBranch",
        "productionBranch"
      ],
      "additionalProperties": false,
      "properties": {
        "mode": { "enum": ["managed", "attached"] },
        "url": { "type": "string", "minLength": 1 },
        "path": { "$ref": "#/$defs/relativePath" },
        "integrationBranch": { "type": "string", "minLength": 1 },
        "productionBranch": { "type": ["string", "null"] },
        "stableWorktree": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        },
        "branchPrefix": { "type": "string", "minLength": 1 }
      },
      "allOf": [
        {
          "if": {
            "type": "object",
            "properties": { "mode": { "const": "managed" } },
            "required": ["mode"]
          },
          "then": {
            "properties": { "stableWorktree": {}, "branchPrefix": {} },
            "required": ["stableWorktree", "branchPrefix"]
          },
          "else": {
            "not": {
              "anyOf": [
                { "required": ["stableWorktree"] },
                { "required": ["branchPrefix"] }
              ]
            }
          }
        }
      ]
    },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?![\\\\/])(?![A-Za-z]:[\\\\/])(?!.*(?:^|[\\\\/])\\.\\.(?:[\\\\/]|$)).+$"
    },
    "knowledgeDirectory": {
      "allOf": [
        { "$ref": "#/$defs/relativePath" },
        { "not": { "const": "." } },
        {
          "not": {
            "type": "string",
            "pattern": "^(?:\\.agents|\\.git|\\.qmd|AGENTS\\.md|CLAUDE\\.md|braingraph\\.json|braingraph\\.local\\.json|repositories|schemas)(?:[\\\\/]|$)"
          }
        }
      ]
    }
  }
}
